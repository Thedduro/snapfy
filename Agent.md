# Agent Working Guide

## 프로젝트 개요

- 이 프로젝트는 친구들과 함께 날짜별 사진과 1분 이내 동영상을 모아보는 공유형 미디어 캘린더 앱이다.
- 핵심 흐름은 `캘린더에서 날짜 선택 -> 카메라 또는 갤러리로 미디어 추가 -> 해당 날짜 셀에 썸네일 표시`이다.
- 현재 우선 구현 범위는 iOS 앱 MVP이며, 위젯, 공유 링크, 협업 기능으로 확장할 수 있는 구조를 목표로 한다.

## 작업 원칙

- 모든 구현은 확장성과 유지보수성을 우선으로 고려한다.
- 임시방편 코드, 중복 코드, 쓰이지 않는 코드, 의미 없는 추상화는 추가하지 않는다.
- 한 파일에 여러 책임을 몰아넣지 말고, 기능 단위와 책임 단위로 파일을 분리한다.
- 새로운 코드 추가 전 기존 구조와 중복되는 책임이 있는지 먼저 확인한다.
- 동일한 로직이 두 번 이상 필요하면 공용 유틸 또는 공용 컴포넌트로 정리한다.
- 화면 코드에는 화면 책임만 두고, 데이터 처리와 변환 로직은 분리한다.
- 구현 후에는 지금 구조가 이후 공유 기능, 위젯, 미디어 상세 화면으로 자연스럽게 확장 가능한지 점검한다.

## 코드 작성 지침

- 파일명과 타입명은 역할이 바로 드러나게 작성한다.
- 기능 단위로 폴더를 정리하고, 관련 없는 코드를 한 디렉터리에 섞지 않는다.
- 사용하지 않는 프로퍼티, 함수, 모델, 주석은 남기지 않는다.
- 중복되는 상수, 문자열, 레이아웃 값은 필요한 범위에서만 공통화한다.
- 과도한 추상화는 피하고, 실제 중복이 발생했을 때만 추상화한다.
- 저장 구조, 권한 처리, 미디어 처리처럼 변경 가능성이 높은 코드는 분리된 파일에 둔다.
- 뷰 파일에서 비대한 로직이 생기면 별도 타입 또는 유틸로 분리한다.
- 기능 구현 시 정상 흐름뿐 아니라 반복 사용, 재진입, 취소, 빈 상태를 함께 고려한다.

## GitHub 작업 컨벤션

- 작업은 기능 단위로 작게 나누고, 의미 있는 단위마다 커밋한다.
- 커밋 메시지는 반드시 한국어로 작성한다.
- 커밋 타입은 아래 기준을 따른다.
  - `feat`: 사용자 기능 추가 또는 UX 확장
  - `fix`: 버그 수정 또는 잘못된 동작 보정
  - `refactor`: 동작 변화 없는 구조 개선
  - `docs`: 문서 추가 또는 수정
  - `chore`: 설정, 정리, 유지 작업
- 커밋 메시지 형식은 아래를 따른다.
  - `feat: 캘린더 날짜 카드 영역 확장`
  - `fix: 미디어 피커 재진입 오류 수정`
  - `docs: 에이전트 작업 지침 추가`
- 하나의 커밋에는 하나의 목적만 담는다.
- 여러 성격의 수정이 섞이면 커밋을 분리한다.
- 커밋 전에는 불필요한 파일과 사용자 환경 파일이 포함되지 않았는지 확인한다.

## 리뷰 기준

- 이 코드가 한 달 뒤에도 바로 읽히는가
- 기능이 늘어나도 현재 파일 구조가 버티는가
- 같은 책임의 코드가 여러 군데 흩어져 있지 않은가
- 지금 추가한 코드가 이후 위젯, 공유 기능, 미디어 상세 기능과 충돌하지 않는가
- 지워야 할 코드가 남아 있지 않은가

## 금지 사항

- 쓰지 않는 코드 미리 작성
- 중복 파일 생성
- 같은 의미의 유틸 중복 생성
- 한 파일에 모델, 뷰, 저장 로직을 전부 혼합
- 테스트하지 않은 임시 코드 유지
- 이유 없는 네이밍 축약

## 기본 태도

- 구현보다 구조를 먼저 본다.
- 빠르게 만들더라도 지저분하게 만들지 않는다.
- MVP라도 이후 확장이 막히는 구조는 피한다.
- 새로운 기능을 넣을 때는 기존 코드 정리 기회도 함께 본다.
